# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

# ANTIGRAVITY CODESMITH SKILLS

Follow these strict validation steps before committing any changes:

1. Always run `npm run lint` to automatically check and fix code style violations on modified files.
2. Always run `npm run format` to enforce Prettier formatting.
3. Pre-commit hooks are configured via Husky and `lint-staged` to block commits containing errors.
4. Keep TypeScript compilation checks clean.
